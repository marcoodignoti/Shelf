<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "https://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!--
    Required: Electron Framework is a separately-signed dylib. Without this
    entitlement, macOS enforces Team ID matching between the main binary and
    loaded libraries. Under ad-hoc signing (no real Team ID) this causes a
    DYLD crash at launch ("different Team IDs"). Only removable once the app
    is signed with a Developer ID so all binaries share one Team ID.
  -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
